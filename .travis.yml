sudo: required
services:
  - docker

# Building all docker container based on 64 bit image
script:
  - make caddy
  - make postgres
  - make monit
  - travis_retry make architecture=amd64 src
  - travis_retry make architecture=armhf src
  - docker images
